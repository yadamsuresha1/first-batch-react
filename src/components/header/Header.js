import "./Header.css";
function Header() {
  //   const appStyle = { backgroundColor: "orange", textAlign: "center" };
  return <h1 className="header">First batch is best batch</h1>;
}

export default Header;
